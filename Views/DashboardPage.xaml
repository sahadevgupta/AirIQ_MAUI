<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AirIQ.Views.DashboardPage"
             xmlns:controls="clr-namespace:AirIQ.Controls"
             Title="DashboardPage">

    <Grid RowDefinitions="Auto,Auto">
        <controls:AdvSegmentedControl ItemsSource="{Binding TabOptions}"
                                      Margin="0,10,0,0"
                                      Grid.Row="0"
                                      DisplayMemberPath="Name"
                                      x:Name="segmentedControl"/>

        <controls:ExtendedDropdown x:Name="filterDropdown"
                                   Title="Select Filter"
                                   ItemsSource="{Binding FilterOptions}"
                                   SelectedItem="{Binding SelectedFilterOption}"
                                   Grid.Row="1"/>


    </Grid>
    
</ContentPage>